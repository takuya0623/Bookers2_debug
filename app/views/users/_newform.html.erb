<%= form_for(book) do |f| %>
<h4>Title</h4>
<%= f.text_field :title %>
<h4>Body</h4>
<%= f.text_area :body %>

<div class="text-left">
<%= f.submit 'Create Book',class: "btn-primary" %>
</div>
<% end %>